# Optional legacy-compatible default profile. The application stores only this
# reference name; the secret value stays in the process environment.
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-5-mini
LLM_API_KEY=

# Add any references used by profiles in the model configuration center.
MODEL_RESEARCH_KEY=
MODEL_VERIFY_KEY=
MODEL_WRITER_KEY=
MODEL_CHEAP_KEY=
