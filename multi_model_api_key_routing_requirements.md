# 多模型 API Key 与任务路由开发需求

## 1. 目标

在现有博士开题助手中增加多模型配置和任务路由能力，使不同研究任务可以调用不同的大语言模型及 API Key。

本功能应兼容现有的 OpenAI-compatible API 调用方式，不改变现有研究设计、证据门控、文献库和版本控制功能。

## 2. 预期任务分工

系统至少支持以下任务类型，并允许分别绑定不同模型：

| 任务类型 | 任务标识 | 建议用途 |
|---|---|---|
| 文献检索 | `literature_search` | 检索候选文献、关键词扩展 |
| 文献摘要 | `literature_summary` | 对已获得的文献做初步摘要 |
| 证据核验 | `evidence_verification` | 核验 DOI、摘要、全文和具体论断 |
| 中文研究设计 | `chinese_research_design` | 研究问题、理论框架和实验设计 |
| 英文学术写作 | `english_academic_writing` | 生成英文开题和论文草稿 |
| 引用检查 | `citation_validation` | 检查论断与文献证据的对应关系 |
| 翻译 | `translation` | 中英文互译和术语统一 |
| 格式整理 | `formatting` | 摘要压缩、结构整理和格式处理 |

示例模型分工：

- 文献检索和初步摘要：Research Model
- DOI、摘要和证据核验：Verification Model
- 中文研究设计：Chinese Research Model
- 英文论文写作：English Writing Model
- 翻译和格式整理：Utility Model

模型分工不应写死在代码中，应由用户在配置界面中修改。

## 3. 模型配置中心

增加“模型配置中心”，每个模型配置至少包含：

- 配置唯一 ID
- 配置名称
- 服务商名称
- API Base URL
- 模型名称
- API Key 引用名称
- 是否启用
- 优先级
- 备注

建议使用如下环境变量保存密钥：

```env
MODEL_RESEARCH_KEY=...
MODEL_VERIFY_KEY=...
MODEL_WRITER_KEY=...
MODEL_CHEAP_KEY=...
```

模型配置中只保存 API Key 的引用名称，例如 `MODEL_RESEARCH_KEY`，不要保存明文密钥。

## 4. API Key 安全要求

必须满足以下要求：

1. API Key 不得写入前端代码、浏览器响应、Markdown、BibTeX 或其他导出文件。
2. API Key 不得出现在日志、错误堆栈、调试输出或审计记录中。
3. 优先使用环境变量或本地安全存储。
4. 页面只能显示“已配置/未配置”或经过遮罩的密钥状态。
5. 连接测试和异常信息不得泄露完整 API Key。
6. README 只说明环境变量名称和配置方法，不包含真实密钥。
7. API 调用必须在服务端完成，不能让浏览器直接获取 API Key。

## 5. 任务路由

增加“任务路由配置”，将每个 `taskType` 绑定到一个默认模型，并可配置备用模型。

路由要求：

- 每种任务有一个默认模型配置。
- 每种任务可以有一个或多个备用模型。
- 默认模型停用或调用失败时，按照优先级尝试备用模型。
- 支持单次任务显式传入 `profileId`，覆盖默认路由。
- 路由配置应保存在本地数据库中。
- 不同任务可以使用同一个模型配置。

## 6. AI 调用接口修改

修改现有生成接口，使请求支持以下参数：

```json
{
  "taskType": "english_academic_writing",
  "profileId": "optional-profile-id",
  "prompt": "...",
  "context": {}
}
```

行为要求：

- 提供 `profileId` 时，优先使用该模型。
- 未提供 `profileId` 时，根据 `taskType` 使用默认路由。
- 默认模型失败时，自动尝试备用模型。
- 返回结果中显示本次实际使用的模型配置名称和模型名，但不返回 API Key。
- 记录任务类型、模型配置名称、模型名、耗时、响应状态和错误状态。
- 不记录完整提示词中的敏感信息，除非现有审计设计明确允许。
- 继续执行现有证据门控：模型不得自由生成未经证据库支持的作者、DOI 或参考文献。

## 7. 用户界面

在设置或工作台中增加以下功能：

- 新增模型配置。
- 编辑模型配置。
- 启用或停用模型配置。
- 删除未使用的模型配置。
- 测试单个模型连接。
- 为不同任务选择默认模型。
- 配置备用模型及优先级。
- 在生成前显示预计使用的模型。
- 支持单次任务临时切换模型。
- 显示本次实际使用的模型和失败切换情况。

界面中不得显示完整 API Key。

## 8. 与现有研究功能的兼容要求

实现过程中必须保留：

- v0.4 研究设计，不得恢复旧版 3×2 实验设计；
- 文献证据状态和核验等级；
- 论断引用完整性校验；
- 英文生成的证据门控；
- Markdown 和 BibTeX 导出；
- SQLite 本地持久化和已有数据迁移；
- 当前的 OpenAlex 检索和候选文献导入。

不得把 OpenAlex 检索结果自动标记为已核验，也不得把 AI 意向指标写成真实销售转化率。

## 9. 测试与验收标准

至少增加或执行以下测试：

1. 可以配置两个以上不同模型。
2. 不同 `taskType` 能够路由到预期模型。
3. 显式 `profileId` 可以覆盖默认路由。
4. 默认模型失败时可以切换备用模型。
5. 停用模型不会被选为默认或备用模型。
6. 未配置 API Key 时返回清晰、可操作的错误。
7. API Key 不出现在前端页面、网络响应、日志和导出文件中。
8. 连接测试能够区分配置错误、认证失败和服务不可用。
9. 现有文献、验证、导出和英文生成测试继续通过。
10. 更新 README，给出环境变量示例、模型配置示例和任务路由说明。

## 10. 实现建议

建议将功能拆分为以下模块：

- `model_profiles`：模型配置和启停状态；
- `task_routes`：任务到模型的默认及备用路由；
- `secret_resolver`：服务端读取环境变量或本地安全存储；
- `model_router`：根据任务类型和优先级选择模型；
- `provider_client`：统一处理 OpenAI-compatible API 请求；
- `generation_audit`：记录模型调用元数据，不记录密钥。

实现时优先复用现有存储、校验和 API 客户端，不要重复建立第二套配置机制。

## 11. 交付要求

完成后请提供：

- 修改后的代码；
- 数据库迁移说明；
- 环境变量配置说明；
- README 更新；
- 自动化测试结果；
- 多模型路由的使用示例；
- 明确说明当前支持的模型供应商和限制。

